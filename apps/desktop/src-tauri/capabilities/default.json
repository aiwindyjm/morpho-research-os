{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default window capability; custom app commands need no extra permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
