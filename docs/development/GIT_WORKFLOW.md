# Git Workflow
Use short-lived branches `feat/<scope>`, `fix/<scope>`, `docs/<scope>`. Conventional commits and SemVer releases; keep changelog entries user-facing. Pull requests state problem, behavior, scope, tests, migrations, and screenshots for UI. Avoid Git Flow complexity.
