# Release
Tag `vMAJOR.MINOR.PATCH`. Record app, schema, worker, prompt, and vault schema versions. SQLite migrations are forward-only and numbered. Release checks run build, typecheck, lint, unit, contract, and packaging tests.
