# Coding Guidelines
Prefer small typed functions, explicit boundaries, deterministic normalization, and dependency injection. Avoid speculative abstractions and hidden global state. Errors are structured with code, user message, developer detail, retryable flag, and correlation ID.
