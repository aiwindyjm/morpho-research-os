# Mock Infrastructure
`MockLLMProvider` and `MockSearchProvider` are mandatory dependency-injected adapters. Golden cases under `examples/fixtures/{quantum-entanglement,brain-computer-interface,large-language-model}` contain input, expected plan, entities, claims, and Markdown snapshots. Offline pipeline tests must run without network or API keys.
